import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'reconocimiento_facial_frontend',
  webDir: 'www'
};

export default config;
